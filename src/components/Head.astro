d---
const siteInfo = {
  siteName: 'Galacticraft',
  siteDesc: 'Read the latest dev blogs from the Galacticraft Team.',
}

    <title>Galacticraft</title>
    <meta content="News - Galacticraft" property="og:title" />
    <meta content="Read the latest dev blogs from the Galacticraft Team." property="og:description" />
    <meta content="https://galacticraft-test.netlify.app/" property="og:url" />
    <meta content="https://cdn.discordapp.com/attachments/804625345718255656/944660077443432448/Rocket.png" property="og:image" />

let {title} = Astro.props;
let {desc} = Astro.props;
let {img} = Astro.props;
let {twitterUsername} = Astro.props;

if(!title) {
  title = siteInfo.siteName
}
if(!desc) {
  desc = siteInfo.siteDesc
}
if(!img) {
  img = Astro.site+'card.png'
}
---

<head>
  
	<!-- essential -->
	<meta charset="utf-8" />
	<meta name="viewport" content="width=device-width" />
  
  <!-- core -->
	{title && <title>{title}</title>}
	{desc && <meta name="description" content={desc}>}
	<!-- twitter -->
	<meta name="twitter:card" content="summary_large_image" />
	{title && <meta name="twitter:title" content={title} />}
	{desc && <meta name="twitter:description" content={desc} />}
	<meta name="twitter:image" content={img} />
	{title && <meta name="twitter:image:alt" content={title} />}
	{twitterUsername && <meta name="twitter:site" content={twitterUsername}/>}
	{twitterUsername && <meta name="twitter:creator" content={twitterUsername}/>}
	<!-- og -->
	{title && <meta property="og:title" content={title} />}
	{desc && <meta property="og:description" content={desc} />}
	<meta property="og:image" content={img} />
	{title && <meta property="og:image:alt" content={title} />}
	{title && <meta property="og:site_name" content={title} />}
	<meta property="og:type" content="object" />
	<meta property="og:url" content={Astro.site} />
	<slot/>
</head>
